<%= form_with(model: offer, local: true) do |form| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= form.hidden_field :profile_id, :value => params[:profile_id]%>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :offer_date %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_area :detail, id: :offer_detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
